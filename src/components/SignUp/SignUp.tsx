import React from "react";

const SignUp = () => (
  <div>
    <h1>SignUp</h1>
    <SignUpForm />
  </div>
);

const SignUpForm = () => {
  const onSubmit = () => {};

  const onChange = () => {};

  return <form onSubmit={onSubmit} />;
};

export default SignUp;
